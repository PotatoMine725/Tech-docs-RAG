# ADR-0001 Initial decisions (SETUP-001)

Accepted: Python; PySide6; ChromaDB; Google Gemini API; pytest; Gemini key via environment variable; format-independent document architecture; 24 accepted / 4 excluded corpus structure (#25 never existed); RAG architecture; evaluation and experiments as first-class concerns.

Open: Gemini model, embedding provider, chunking strategy, experiment choice, ChromaDB path/commit policy.
