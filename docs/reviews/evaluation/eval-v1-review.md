# EVAL-002 human review: eval-v1 questions and ground truth

Status: **waiting for the owner's review** (drafted by AI, unattended run 2026-09-25). Nothing is frozen: no `eval-freeze-v1` tag, no final snapshot.
Files under review: [`eval-v1.jsonl`](../../../data/evaluation/questions/eval-v1.jsonl) (36 cases), [`dev-v1.jsonl`](../../../data/evaluation/questions/dev-v1.jsonl) (6 cases).
Only the **question wording** is new in EVAL-002 ([`question-wording-v1.yaml`](../../../data/evaluation/questions/question-wording-v1.yaml)). Every other field (answer points, sources, slots, evidence, criteria) is copied unchanged from the approved `blueprint.yaml` by `scripts/evaluation/build_question_files.py`; a test fails if the files differ from that build.
Automatic checks already passed (`scripts/evaluation/validate_questions.py`, `tests/unit/test_eval_dataset.py`): schema, 36/32 answerable, 18 EN / 18 VI, 7 parallel pairs identical, accepted sources only, heading paths in the inventory, all 107 evidence quotes found verbatim in the normalized section text, eval/dev disjoint. What a script cannot judge is below.

## How to review (≈ 20 min)
1. Read "Look at these first".
2. Skim the table with the checklist; tick **OK?** or write the fix in the row.
3. Answer the AI decisions and the ground-truth proposals (yes/no each).
4. Fill the verdict line at the bottom. Fixes to wording go into `question-wording-v1.yaml`; fixes to ground truth go into `blueprint.yaml`. Then run `build_question_files.py`, `validate_questions.py`, `build_review_sheet.py` and pytest.

## (a) Checklist for every row
- [ ] **Vietnamese sounds natural**: something a Vietnamese developer would type; English kept only for API names and common terms (DbContext, middleware, route, unit test, Assert).
- [ ] **The question does not leak the answer or the exact name to look up**: it describes the need or the symptom; it does not copy the evidence sentence or the heading it is filed under (exceptions the blueprint asked for: API names in 017, 022, 023, 032; the code/error text in 016, 018, 019, 020, 026).
- [ ] **The expected answer matches the evidence quote**: each required point (bold) is really stated by a quote in the same row.
- [ ] EN and VI of a parallel group (PG-00x) ask the same thing.

## (b) Look at these first (AI least sure)
| Case | Why |
|---|---|
| Q-EVAL-024 (vi) | Names `Assembly.Location`, a word from the evidence sentence ("tests that load files relative to Assembly.Location"). It is the natural symptom, but it may make retrieval easier than intended. Alternative: "load file nằm cạnh file DLL của test". The verifier adds: the clause "vì test không chạy trong thư mục output" gives away the cause half of P1; suggest dropping it. |
| Q-EVAL-003 / 004 | Says "older templates" / "template cũ", which hints that older templates behave differently (P2). Without it the question can't ask for P2 without naming `UseDeveloperExceptionPage`. Also names the page (the heading) — unavoidable. |
| Q-EVAL-017 | "what does the IMiddleware one need so the framework can create it" is meant to draw out P3 (registered as scoped/transient; IMiddlewareFactory). It may read as vague; P3 is required. |
| Q-EVAL-028 (vi) | "chạy nhanh và gọn ngang ngửa C hay assembly" is close to the evidence ("compete directly on performance and size with C or assembly"). Is "gọn" (size) too leading, and is the phrasing natural? |
| Q-EVAL-030 (vi) | Long; asks for the test name *and* the verdict. Check it does not give the name away and that "quy tắc đặt tên unit test" sounds natural. |
| Q-EVAL-016 (vi) | Snippet from #26 with the output comment `// 4 — same object` removed; the `WriteLine` text was kept as on the page. Check no hint remains. |
| Q-EVAL-025 | "cheaply end these requests" is vaguer than the page ("short-circuit"). A correct answer must still name `MapShortCircuit`; check the wording steers there fairly. |
| Q-EVAL-026 (vi) | Quotes the NU1202 message, which contains "is not compatible with net5.0" (P1). The blueprint asked for the message; flagging because it gives P1 away. |
| Q-EVAL-023 | "Can I change that?" signals that a switch exists (P3). Drop it if it leaks too much. |

## AI decisions to confirm (none changes ground truth)
Full list with reasons: [`session-handoff-2026-09-25-eval-002.md`](../../plans/session-handoff-2026-09-25-eval-002.md) § AI decisions.
1. Case IDs `Q-EVAL-001…036` / `Q-DEV-001…006` (not `EVAL-001`, which is a task ID); `blueprint_id` links each case to its blueprint.
2. `expected_answer` = the required answer points joined, in **English** for both languages (ground truth is shared by parallel pairs; answers are judged by meaning). Unanswerable cases: the refusal sentence.
3. `tags {difficulty, cognitive_level, size_class, failure_mode, scope}` (09a runner) **and** the same values as flat fields (design §18); the validator checks they are equal.
4. Generated fields kept in a separate `generated` object, all null (`generated_answer`, `citations`, `result`); real outputs go to per-arm result files (design §18).
5. Extra copied fields: `evidence[].supports`, `near_miss_sources`, `absence_proof`, alternate `note`.
6. Quote check runs on the normalized corpus (`normalized.jsonl`, ADR-0003 D1) with whitespace runs collapsed on both sides, inside the quote's own section (any variant).

## Ground-truth proposals (G1 approved and applied by the owner, 2026-09-26)
| # | Case | Proposal | Why |
|---|---|---|---|
| G1 | 011/012 | Reword `must_not_claim` "The type must be public (internal is also allowed)." → "The type must be public (wrong: internal is also allowed)." | As written, a judge could read the parenthesis as part of the forbidden claim or as a permission. Meaning unchanged. |

## Verdict
owner verdict (approve / approve with fixes / reject): approve with fixes (owner, 2026-09-26): 024, 017, 028 wording; G1; A1–A10 approved
owner notes: 

<!-- CASES START -->
### Evaluation set (36)

| ID | Lang | Group | Question | Expected answer (required points bold) | Expected source · heading path | Evidence quote(s) | OK? |
|---|---|---|---|---|---|---|---|
| Q-EVAL-001 | en | PG-001 | If the entities my EF Core query returns are already loaded in the DbContext, does EF Core still send the query to the database, or does it just hand me the objects it already has? | **P1**: No. EF Core always executes the query against the database, even if the entities returned by the query already exist in the context.<br>P2 (opt): EF Core uses LINQ; it passes a representation of the LINQ query to the database provider, which translates it to a database-specific query language such as SQL. | **S1** #22 Querying Data | [P1] #22: "Queries are always executed against the database even if the entities returned in the result already exist in the context."<br>[P2] #22: "EF Core passes a representation of the LINQ query to the database provider. Database providers in turn translate it to database-specific query language (for example, SQL for a relational database)." | ☐ |
| Q-EVAL-002 | vi | PG-001 | Nếu các entity mà query EF Core trả về đã được load sẵn trong DbContext rồi thì EF Core có còn gửi query xuống database nữa không, hay lấy luôn các object đang có? | **P1**: No. EF Core always executes the query against the database, even if the entities returned by the query already exist in the context.<br>P2 (opt): EF Core uses LINQ; it passes a representation of the LINQ query to the database provider, which translates it to a database-specific query language such as SQL. | **S1** #22 Querying Data | [P1] #22: "Queries are always executed against the database even if the entities returned in the result already exist in the context."<br>[P2] #22: "EF Core passes a representation of the LINQ query to the database provider. Database providers in turn translate it to database-specific query language (for example, SQL for a relational database)." | ☐ |
| Q-EVAL-003 | en | PG-002 | How does the developer exception page get turned on in an ASP.NET Core app? Does a newly created app get it automatically, what do apps built from the older templates have to do, and in which environment should it be active? | **P1**: It is enabled by default when both: the app runs in the Development environment, and the app was created with the current templates (WebApplication.CreateBuilder).<br>**P2**: Apps created with earlier templates (WebHost.CreateDefaultBuilder) enable it by calling app.UseDeveloperExceptionPage.<br>**P3**: It should only be enabled in the Development environment; detailed exception information must not be shown publicly in production.<br>P4 (opt): It can show the stack trace, query string parameters, cookies and headers. | **S1** #13 Handle errors in ASP.NET Core > Developer exception page<br>alt S1 #13 Handle errors in ASP.NET Core > Developer Exception Page<br>alt S1 #12 Handle errors in ASP.NET Core APIs > Developer Exception Page | [P1] #13: "ASP.NET Core apps enable the developer exception page by default when both:"<br>[P1] #13: "Running in the [`Development` environment](environments?view=aspnetcore-10.0)."<br>[P1] #13: "The app was created with the current templates, that is, by using [WebApplication.CreateBuilder](/en-us/dotnet/api/microsoft.aspnetcore.builder.webapplication.createbuilder)."<br>[P2] #13: "Apps created using earlier templates, that is, by using [WebHost.CreateDefaultBuilder](/en-us/dotnet/api/microsoft.aspnetcore.webhost.createdefaultbuilder), can enable the developer exception page by calling"<br>[P3] #13: "Don't enable the Developer Exception Page **unless the app is running in the `Development` environment**. Don't share detailed exception information publicly when the app runs in production."<br>[P3] #13: "The preceding code enables the Developer Exception Page ***only*** when the app runs in the `Development` environment." | ☐ |
| Q-EVAL-004 | vi | PG-002 | Trong ASP.NET Core, Developer Exception Page được bật như thế nào? App mới tạo có tự bật sẵn không, app tạo từ template cũ thì phải làm gì, và chỉ nên bật nó ở môi trường nào? | **P1**: It is enabled by default when both: the app runs in the Development environment, and the app was created with the current templates (WebApplication.CreateBuilder).<br>**P2**: Apps created with earlier templates (WebHost.CreateDefaultBuilder) enable it by calling app.UseDeveloperExceptionPage.<br>**P3**: It should only be enabled in the Development environment; detailed exception information must not be shown publicly in production.<br>P4 (opt): It can show the stack trace, query string parameters, cookies and headers. | **S1** #13 Handle errors in ASP.NET Core > Developer exception page<br>alt S1 #13 Handle errors in ASP.NET Core > Developer Exception Page<br>alt S1 #12 Handle errors in ASP.NET Core APIs > Developer Exception Page | [P1] #13: "ASP.NET Core apps enable the developer exception page by default when both:"<br>[P1] #13: "Running in the [`Development` environment](environments?view=aspnetcore-10.0)."<br>[P1] #13: "The app was created with the current templates, that is, by using [WebApplication.CreateBuilder](/en-us/dotnet/api/microsoft.aspnetcore.builder.webapplication.createbuilder)."<br>[P2] #13: "Apps created using earlier templates, that is, by using [WebHost.CreateDefaultBuilder](/en-us/dotnet/api/microsoft.aspnetcore.webhost.createdefaultbuilder), can enable the developer exception page by calling"<br>[P3] #13: "Don't enable the Developer Exception Page **unless the app is running in the `Development` environment**. Don't share detailed exception information publicly when the app runs in production."<br>[P3] #13: "The preceding code enables the Developer Exception Page ***only*** when the app runs in the `Development` environment." | ☐ |
| Q-EVAL-005 | en | PG-003 | My C# app has two jobs: downloading a file from a web service, and running an expensive calculation on the downloaded data. Should both be written the same way with async/await, or does one of them need to be handled differently? | **P1**: I/O-bound work (waiting for data, e.g. from a database or web service): use async and await without Task.Run.<br>**P2**: CPU-bound work (an expensive computation): use async and await, but run the work on another thread with Task.Run.<br>P3 (opt): For work suited to concurrency and parallelism, also consider the Task Parallel Library.<br>P4 (opt): Measure: CPU-bound work may not be costly enough to outweigh the overhead of context switches. | **S1** #02 Asynchronous programming scenarios > Recognize CPU-bound and I/O-bound scenarios<br>alt S1 #02 Asynchronous programming scenarios > Explore the asynchronous programming model > CPU-bound example: Run game calculation<br>alt S1 #02 Asynchronous programming scenarios > Explore the asynchronous programming model > I/O-bound example: Download data from web service<br>alt S1 #02 Asynchronous programming scenarios > Explore the asynchronous programming model | [P1] #02: "Use the `async` modifier and `await` expression *without* the `Task.Run` method."<br>[P2] #02: "Use the `async` modifier and `await` expression, but spawn off the work on another thread with the `Task.Run` method."<br>[P4] #02: "Always measure the execution of your code." | ☐ |
| Q-EVAL-006 | vi | PG-003 | App C# của mình có 2 việc: tải một file từ web service, và chạy một phép tính rất nặng trên dữ liệu vừa tải về. Hai việc này viết bằng async/await giống hệt nhau được không, hay có việc nào phải xử lý khác đi? | **P1**: I/O-bound work (waiting for data, e.g. from a database or web service): use async and await without Task.Run.<br>**P2**: CPU-bound work (an expensive computation): use async and await, but run the work on another thread with Task.Run.<br>P3 (opt): For work suited to concurrency and parallelism, also consider the Task Parallel Library.<br>P4 (opt): Measure: CPU-bound work may not be costly enough to outweigh the overhead of context switches. | **S1** #02 Asynchronous programming scenarios > Recognize CPU-bound and I/O-bound scenarios<br>alt S1 #02 Asynchronous programming scenarios > Explore the asynchronous programming model > CPU-bound example: Run game calculation<br>alt S1 #02 Asynchronous programming scenarios > Explore the asynchronous programming model > I/O-bound example: Download data from web service<br>alt S1 #02 Asynchronous programming scenarios > Explore the asynchronous programming model | [P1] #02: "Use the `async` modifier and `await` expression *without* the `Task.Run` method."<br>[P2] #02: "Use the `async` modifier and `await` expression, but spawn off the work on another thread with the `Task.Run` method."<br>[P4] #02: "Always measure the execution of your code." | ☐ |
| Q-EVAL-007 | en | PG-004 | My ASP.NET Core API has a route like `api/products/{id:int}`. When a client sends a malformed id such as `abc`, it gets a 404 instead of an error saying the id is invalid. Why does that happen, and is relying on the route like this a good way to validate input? | **P1**: Route constraints shouldn't be used for input validation.<br>**P2**: If they are, invalid input produces 404 Not Found; invalid input should instead produce 400 Bad Request with an appropriate error message.<br>**P3**: Route constraints are meant to disambiguate similar routes.<br>P4 (opt): Constraints run after the URL matched and was tokenized into route values; they make a true/false decision about a route value. | **S1** #23 Routing in ASP.NET Core > Route constraints<br>alt S1 #23 Routing in ASP.NET Core > Route constraint reference | [P1,P2] #23: "Don't use constraints for input validation. If constraints are used for input validation, invalid input results in a `404` Not Found response."<br>[P2,P3] #23: "Invalid input should produce a `400` Bad Request with an appropriate error message. Route constraints are used to disambiguate similar routes, not to validate the inputs for a particular route." | ☐ |
| Q-EVAL-008 | vi | PG-004 | API ASP.NET Core của mình có route kiểu `api/products/{id:int}`. Khi client gửi id sai định dạng như `abc` thì nhận về 404 chứ không phải lỗi báo id không hợp lệ. Tại sao lại vậy, và dựa vào route như thế để validate input có ổn không? | **P1**: Route constraints shouldn't be used for input validation.<br>**P2**: If they are, invalid input produces 404 Not Found; invalid input should instead produce 400 Bad Request with an appropriate error message.<br>**P3**: Route constraints are meant to disambiguate similar routes.<br>P4 (opt): Constraints run after the URL matched and was tokenized into route values; they make a true/false decision about a route value. | **S1** #23 Routing in ASP.NET Core > Route constraints<br>alt S1 #23 Routing in ASP.NET Core > Route constraint reference | [P1,P2] #23: "Don't use constraints for input validation. If constraints are used for input validation, invalid input results in a `404` Not Found response."<br>[P2,P3] #23: "Invalid input should produce a `400` Bad Request with an appropriate error message. Route constraints are used to disambiguate similar routes, not to validate the inputs for a particular route." | ☐ |
| Q-EVAL-009 | en | PG-005 | One of my unit tests calls the method under test twice, with two different inputs, and then asserts both results at the end. Is that a problem, and how should I structure the test instead? | **P1**: Include only one Act per test (for example a separate test per Act, or a parameterized test).<br>**P2**: Multiple Acts need to be asserted individually, and you can't guarantee that all Asserts execute.<br>**P3**: At least one benefit of a single Act: you can see which Act fails, the test stays focused on one case, or you get a clear picture of why it fails.<br>P4 (opt): In most unit testing frameworks, after an Assert fails, all subsequent tests are considered failing, so working functionality can look broken. | **S1** #03 Unit testing best practices for .NET > Best practices > Avoid multiple Act tasks | [P1] #03: "When you write your tests, try to include only one Act task per test."<br>[P2] #03: "Multiple Act tasks need to be individually asserted, and you can't guarantee that all Assert tasks execute."<br>[P4] #03: "In most unit testing frameworks, after an Assert task fails in a unit test, all subsequent tests are automatically considered as failing."<br>[P3] #03: "You can easily discern which Act task is failing if the test fails." | ☐ |
| Q-EVAL-010 | vi | PG-005 | Một unit test của mình gọi method cần test hai lần với hai input khác nhau, rồi cuối cùng mới Assert cả hai kết quả. Viết như vậy có vấn đề gì không, và nên tổ chức lại test thế nào? | **P1**: Include only one Act per test (for example a separate test per Act, or a parameterized test).<br>**P2**: Multiple Acts need to be asserted individually, and you can't guarantee that all Asserts execute.<br>**P3**: At least one benefit of a single Act: you can see which Act fails, the test stays focused on one case, or you get a clear picture of why it fails.<br>P4 (opt): In most unit testing frameworks, after an Assert fails, all subsequent tests are considered failing, so working functionality can look broken. | **S1** #03 Unit testing best practices for .NET > Best practices > Avoid multiple Act tasks | [P1] #03: "When you write your tests, try to include only one Act task per test."<br>[P2] #03: "Multiple Act tasks need to be individually asserted, and you can't guarantee that all Assert tasks execute."<br>[P4] #03: "In most unit testing frameworks, after an Assert task fails in a unit test, all subsequent tests are automatically considered as failing."<br>[P3] #03: "You can easily discern which Act task is failing if the test fails." | ☐ |
| Q-EVAL-011 | en | PG-006 | I put `[ValidatableType]` on a model class that is declared private and nested inside another class. The app builds, but validation simply never runs for that model. What is going on, and how do I fix it? | **P1**: The validation source generator emits code in a separate file, so it can't access a type (or one of its containing types) that is private or file-local.<br>**P2**: In that case the generator silently skips validation for the type (diagnostic ASP0033).<br>**P3**: Fix: make the attributed type and each of its containing types public or internal.<br>P4 (opt): ASP0033 is a Usage rule with severity Warning. | **S1** #29 ASP0033: `[ValidatableType]` is applied to an inaccessible type > Rule description<br>**S1** #29 ASP0033: `[ValidatableType]` is applied to an inaccessible type > How to fix violations<br>alt S1 #29 ASP0033: `[ValidatableType]` is applied to an inaccessible type > Cause | [P1,P2] #29: "The validation source generator emits code in a separate file. A validatable type and all of its containing types must be public or internal and can't be file-local. Otherwise, the generator silently skips validation for the type."<br>[P3] #29: "Make the attributed type and each of its containing types public or internal." | ☐ |
| Q-EVAL-012 | vi | PG-006 | Mình gắn `[ValidatableType]` cho một model class khai báo private, lồng bên trong một class khác. App vẫn build được nhưng validation cho model đó không bao giờ chạy. Nguyên nhân là gì và sửa thế nào? | **P1**: The validation source generator emits code in a separate file, so it can't access a type (or one of its containing types) that is private or file-local.<br>**P2**: In that case the generator silently skips validation for the type (diagnostic ASP0033).<br>**P3**: Fix: make the attributed type and each of its containing types public or internal.<br>P4 (opt): ASP0033 is a Usage rule with severity Warning. | **S1** #29 ASP0033: `[ValidatableType]` is applied to an inaccessible type > Rule description<br>**S1** #29 ASP0033: `[ValidatableType]` is applied to an inaccessible type > How to fix violations<br>alt S1 #29 ASP0033: `[ValidatableType]` is applied to an inaccessible type > Cause | [P1,P2] #29: "The validation source generator emits code in a separate file. A validatable type and all of its containing types must be public or internal and can't be file-local. Otherwise, the generator silently skips validation for the type."<br>[P3] #29: "Make the attributed type and each of its containing types public or internal." | ☐ |
| Q-EVAL-013 | en | PG-007 | My custom ASP.NET Core middleware takes a scoped service as a constructor parameter, and the app throws an exception at runtime. Why does this happen, and what are my options to fix it? | **P1**: Injecting a scoped service through the (conventional) middleware's constructor throws a runtime exception, because it forces the scoped service to behave like a singleton.<br>**P2**: Fix 1: inject the scoped service into the middleware's Invoke or InvokeAsync method instead.<br>**P3**: Fix 2: use factory-based middleware, which is activated per client request, so scoped services can be injected into its constructor. | **S1** #10 Dependency injection in ASP.NET Core > Service lifetimes<br>alt S1 #11 Factory-based middleware activation in ASP.NET Core<br>alt S1 #11 Factory-based middleware activation in ASP.NET Core > Additional resources<br>alt S1 #11 Factory-based middleware activation in ASP.NET Core > IMiddleware<br>alt S1 #10 Dependency injection in ASP.NET Core > Lifetime and registration options | [P1] #10: "Using [constructor injection](../mvc/controllers/dependency-injection?view=aspnetcore-10.0#constructor-injection) throws a runtime exception because it forces the scoped service to behave like a singleton."<br>[P2] #10: "Inject the service into the middleware's `Invoke` or `InvokeAsync` method."<br>[P3] #10: "Middleware registered using this approach is activated per client request (connection), which allows scoped services to be injected into the middleware's constructor." | ☐ |
| Q-EVAL-014 | vi | PG-007 | Middleware tự viết trong ASP.NET Core của mình nhận một service scoped qua constructor, và app bị throw exception lúc runtime. Tại sao lại vậy, và có những cách nào để sửa? | **P1**: Injecting a scoped service through the (conventional) middleware's constructor throws a runtime exception, because it forces the scoped service to behave like a singleton.<br>**P2**: Fix 1: inject the scoped service into the middleware's Invoke or InvokeAsync method instead.<br>**P3**: Fix 2: use factory-based middleware, which is activated per client request, so scoped services can be injected into its constructor. | **S1** #10 Dependency injection in ASP.NET Core > Service lifetimes<br>alt S1 #11 Factory-based middleware activation in ASP.NET Core<br>alt S1 #11 Factory-based middleware activation in ASP.NET Core > Additional resources<br>alt S1 #11 Factory-based middleware activation in ASP.NET Core > IMiddleware<br>alt S1 #10 Dependency injection in ASP.NET Core > Lifetime and registration options | [P1] #10: "Using [constructor injection](../mvc/controllers/dependency-injection?view=aspnetcore-10.0#constructor-injection) throws a runtime exception because it forces the scoped service to behave like a singleton."<br>[P2] #10: "Inject the service into the middleware's `Invoke` or `InvokeAsync` method."<br>[P3] #10: "Middleware registered using this approach is activated per client request (connection), which allows scoped services to be injected into the middleware's constructor." | ☐ |
| Q-EVAL-015 | en | — | Our C# codebase chains asynchronous operations with ContinueWith. Why would we want to rewrite those chains to use await instead? | **P1**: Readability: async/await code reads like synchronous code.<br>**P2**: Error handling: try/catch works naturally with await, while ContinueWith needs careful handling of faulted tasks.<br>**P3**: At least one more advantage: maintainability (adding/removing steps needs minimal changes), debugging (better call stack and debugger experience), or performance (more sophisticated compiler optimizations).<br>P4 (opt): The benefit grows with the number of chained operations (3-4 or more). | **S1** #01 Asynchronous programming with async and await > Async/await vs ContinueWith > Why async/await is preferred<br>alt S1 #01 Asynchronous programming with async and await > Async/await vs ContinueWith | [P1] #01: "**Readability**: The code reads like synchronous code, making it easier to understand the flow of operations."<br>[P2] #01: "**Error handling**: Exception handling with `try`/`catch` blocks works naturally, whereas `ContinueWith` requires careful handling of faulted tasks."<br>[P3] #01: "**Debugging**: The call stack and debugger experience is much better with `async`/`await`." | ☐ |
| Q-EVAL-016 | vi | — | Đoạn code C# này in ra `list1.Count` bằng bao nhiêu, và tại sao?<br>`var list1 = new List<int> { 1, 2, 3 };`<br>`var list2 = list1;`<br>`list2.Add(4);`<br>`Console.WriteLine($"list1 count: {list1.Count}");` | **P1**: list1.Count is 4: List&lt;int> is a reference type, so list1 and list2 point to the same object and the Add through list2 is visible through list1.<br>P2 (opt): Value types (structs such as the Coords record struct, enums, built-in numeric types) copy their data on assignment, so changing one variable doesn't affect the other.<br>P3 (opt): Classes, arrays, delegates and strings are reference types. | **S1** #26 The C# type system > Value types and reference types<br>alt S1 #07 C# classes<br>alt S1 #07 C# classes > Create objects | [P1] #26: "**Reference types** hold a reference to an object on the managed heap. When you assign a reference type to a new variable, both variables point to the same object."<br>[P2] #26: "**Value types** hold their data directly. When you assign a value type to a new variable, the runtime copies the data." | ☐ |
| Q-EVAL-017 | en | — | In ASP.NET Core, what is the difference between how a conventional middleware and a middleware that implements IMiddleware get a scoped DbContext, and what extra setup does the IMiddleware one need in the app? | **P1**: Conventional middleware receives the scoped service (SampleDbContext in the example) as a parameter of InvokeAsync.<br>**P2**: Factory-activated middleware implements IMiddleware and receives the scoped service through its constructor, because IMiddleware is activated per client request (connection).<br>**P3**: The factory-activated middleware is registered as a scoped or transient service in the service container; UseMiddleware sees that the type implements IMiddleware and resolves it with the registered IMiddlewareFactory.<br>P4 (opt): Benefits: activation per client request (injection of scoped services) and strong typing. | **S1** #11 Factory-based middleware activation in ASP.NET Core<br>**S2** #11 Factory-based middleware activation in ASP.NET Core > IMiddleware<br>alt S1 #11 Factory-based middleware activation in ASP.NET Core > Additional resources<br>alt S2 #10 Dependency injection in ASP.NET Core > Service lifetimes | [P2] #11: "[IMiddleware](/en-us/dotnet/api/microsoft.aspnetcore.http.imiddleware) is activated per client request (connection), so scoped services can be injected into the middleware's constructor."<br>[P3] #11: "The middleware is registered as a [scoped or transient service](../dependency-injection?view=aspnetcore-10.0#service-lifetimes) in the app's service container."<br>[P1] #11: "public async Task InvokeAsync(HttpContext context, SampleDbContext dbContext)" | ☐ |
| Q-EVAL-018 | vi | — | Mình muốn kiểm tra ký tự `c` không phải chữ thường nên viết `c is not >= 'a' and &lt;= 'z'`, nhưng kết quả không như mong đợi. Sai ở đâu và sửa thế nào? | **P1**: Pattern combinators bind in the order not, then and, then or; not binds to its operand first.<br>**P2**: So `c is not >= 'a' and &lt;= 'z'` is parsed as `c is ((not >= 'a') and &lt;= 'z')`, which is not "not a lowercase letter".<br>**P3**: Fix: add parentheses, `c is not (>= 'a' and &lt;= 'z')`.<br>P4 (opt): Use parentheses to make patterns clear; the order of checking patterns with the same binding order is undefined. | **S1** #21 Pattern matching - the `is` and `switch` expressions, and operators `and`, `or`, and `not` in patterns > Logical patterns > Precedence and order of checking<br>alt S1 #21 Pattern matching - the `is` and `switch` expressions, and operators `and`, `or`, and `not` in patterns > Parenthesized pattern | [P1] #21: "The `not` pattern binds to its operand first. The `and` pattern binds after any `not` pattern expression binding."<br>[P2] #21: "static bool IsNotLowerCaseLetterDefaultBinding(char c) => c is ((not >= 'a') and &lt;= 'z');"<br>[P3] #21: "static bool IsNotLowerCaseLetterParentheses(char c) => c is not (>= 'a' and &lt;= 'z');" | ☐ |
| Q-EVAL-019 | en | — | In a C# list pattern, what does `..` match, how many times can it appear in one pattern, and is `new[] { 2, 4 } is [.., 2, 4]` true? | **P1**: The slice pattern `..` matches zero or more elements.<br>**P2**: At most one slice pattern can be used in a list pattern, and it can only appear inside a list pattern.<br>**P3**: `new[] { 2, 4 } is [.., 2, 4]` is true.<br>P4 (opt): A subpattern can be nested in a slice, e.g. `.. var s`. | **S1** #21 Pattern matching - the `is` and `switch` expressions, and operators `and`, `or`, and `not` in patterns > List patterns | [P1,P2] #21: "A slice pattern matches zero or more elements. You can use at most one slice pattern in a list pattern. The slice pattern can only appear in a list pattern."<br>[P3] #21: "Console.WriteLine(new[] { 2, 4 } is [.., 2, 4]);  // True" | ☐ |
| Q-EVAL-020 | vi | — | Với `var x = 0xFF_FF_FF_FF;` thì x có kiểu gì và giá trị bao nhiêu? Nếu mình muốn lấy -1 kiểu int từ literal này thì phải viết thế nào? | **P1**: Without a suffix, the literal's type is the first of int, uint, long, ulong that can represent the value.<br>**P2**: So `0xFF_FF_FF_FF` is the uint value 4,294,967,295, not int -1: literals are interpreted as positive values.<br>**P3**: To get -1 as int, cast in an unchecked context: `unchecked((int)0xFF_FF_FF_FF)`.<br>P4 (opt): U/u suffix: first of uint, ulong; L/l suffix: first of long, ulong. | **S1** #16 Integral numeric types (C# reference) > Integer literals | [P1] #16: "If the literal has no suffix, the compiler assigns the type as the first of the following types in which its value can be represented: `int`, `uint`, `long`, `ulong`."<br>[P2] #16: "For example, the literal `0xFF_FF_FF_FF` represents the number `4,294,967,295` of the `uint` type, though it has the same bit representation as the number `-1` of the `int` type."<br>[P3] #16: "For example, `unchecked((int)0xFF_FF_FF_FF)` produces `-1`." | ☐ |
| Q-EVAL-021 | en | — | How can I run a C# program that lives in a single .cs file without creating a project first? | **P1**: Beginning with C# 14 and .NET 10, a program contained in a single .cs file can be run with `dotnet run &lt;file>.cs` (e.g. `dotnet run hello-world.cs`).<br>**P2**: The source must be a single file, but otherwise all C# syntax is valid.<br>P3 (opt): A `#!` (shebang) first line lets Unix run the file directly (e.g. `./hello-world.cs`) when it has execute permission.<br>P4 (opt): Useful for small command-line utilities, prototypes and experiments. | **S1** #20 A tour of the C# language > File-based apps | [P1] #20: "Beginning with C# 14 and .NET 10, you can create *file-based apps*, which simplifies building and running C# programs."<br>[P2] #20: "The source for these programs must be a single file, but otherwise all C# syntax is valid."<br>[P1] #20: "You use the `dotnet run` command to run a program contained in a single `*.cs` file." | ☐ |
| Q-EVAL-022 | vi | — | Trong ASP.NET Core, UseStatusCodePagesWithRedirects và UseStatusCodePagesWithReExecute khác nhau thế nào? Cái nào giữ nguyên status code 404 gốc trả về cho client, và cái nào làm URL trên thanh địa chỉ trình duyệt đổi sang trang lỗi? | **P1**: UseStatusCodePagesWithRedirects sends a 302 Found to the client and redirects it to the error endpoint (which typically returns 200); the original status code is not preserved.<br>**P2**: UseStatusCodePagesWithReExecute re-executes the request pipeline with an alternate path to generate the response body and does not alter the status code, so the original code is returned.<br>**P3**: With redirects the browser address bar shows the error endpoint; with re-execute it shows the originally requested URL.<br>P4 (opt): Both take a URL template with a {0} status-code placeholder; the re-execute template must start with '/'.<br>P5 (opt): Variants 1-4 add that the re-executed pipeline may change the status code; if it doesn't, the original code is sent. | **S1** #13 Handle errors in ASP.NET Core > UseStatusCodePages > UseStatusCodePagesWithRedirects<br>**S2** #13 Handle errors in ASP.NET Core > UseStatusCodePages > UseStatusCodePagesWithReExecute<br>alt S1 #13 Handle errors in ASP.NET Core > UseStatusCodePagesWithRedirects<br>alt S2 #13 Handle errors in ASP.NET Core > UseStatusCodePagesWithReExecute | [P1] #13: "Sends a [302 - Found](https://developer.mozilla.org/docs/Web/HTTP/Status/302) status code to the client."<br>[P2] #13: "Generates the response body by re-executing the request pipeline using an alternate path."<br>[P2] #13: "Does not alter the status code before or after re-executing the pipeline."<br>[P5] #13: "The new pipeline execution may alter the response's status code, as the new pipeline has full control of the status code. If the new pipeline does not alter the status code, the original status code will be sent to the client."<br>[P3] #13: "For web apps, the client's browser address bar reflects the redirected endpoint."<br>[P3] #13: "For web apps, the client's browser address bar reflects the originally requested endpoint." | ☐ |
| Q-EVAL-023 | en | — | When my IExceptionHandler handles an exception and returns true, does ASP.NET Core still write logs and emit metrics for that exception? Can I change that? | **P1**: Starting in .NET 10, diagnostics such as logs and metrics are suppressed by default for handled exceptions (TryHandleAsync returns true).<br>**P2**: In .NET 8 and 9, diagnostics were always emitted, whether or not the exception was handled.<br>**P3**: The default can be changed with SuppressDiagnosticsCallback.<br>P4 (opt): Handlers run in registration order until one returns true; UseExceptionHandler must be called or no handler runs. | **S1** #13 Handle errors in ASP.NET Core > IExceptionHandler<br>alt S1 #13 Handle errors in ASP.NET Core > IExceptionHandler > SuppressDiagnosticsCallback | [P1] #13: "Starting in .NET 10, the default behavior is to suppress emission of diagnostics such as logs and metrics for handled exceptions (when `TryHandleAsync` returns `true`)."<br>[P2] #13: "This differs from earlier versions (.NET 8 and 9) where diagnostics were always emitted regardless of whether the exception was handled."<br>[P3] #13: "The default behavior can be changed by setting [SuppressDiagnosticsCallback](#suppressdiagnosticscallback)."<br>[P1] #13: "Starting in .NET 10, diagnostics are suppressed by default for handled exceptions."<br>[P2,P3] #13: "To revert to the .NET 8 and 9 behavior where diagnostics are always emitted for handled exceptions, set the callback to always return `false`:" | ☐ |
| Q-EVAL-024 | vi | — | Integration test viết bằng xUnit của mình load file theo đường dẫn tương đối với `Assembly.Location`, nhưng lúc chạy test thì không tìm thấy file. Nguyên nhân là gì và cấu hình thế nào để khắc phục? | **P1**: Shadow copying runs the tests in a different directory than the output directory; tests that load files relative to Assembly.Location may run into issues, and you might have to disable shadow copying.<br>**P2**: With xUnit, add an xunit.runner.json file in the test project directory containing "shadowCopy": false. | **S1** #17 Integration tests in ASP.NET Core > Disable shadow copying | [P1] #17: "Shadow copying causes the tests to execute in a different directory than the output directory. If your tests rely on loading files relative to `Assembly.Location` and you encounter issues, you might have to disable shadow copying."<br>[P2] #17: "To disable shadow copying when using xUnit, create a `xunit.runner.json` file in your test project directory"<br>[P2] #17: ""shadowCopy": false" | ☐ |
| Q-EVAL-025 | en | — | Bots keep requesting /robots.txt and /favicon.ico on my ASP.NET Core app, which has neither file, and every one of those requests runs through the whole middleware pipeline, including authentication. How can I cheaply end these requests without running the rest of the pipeline? | **P1**: Use `app.MapShortCircuit(404, "robots.txt", "favicon.ico");` to short-circuit several URL prefixes at once.<br>**P2**: Short-circuiting makes routing invoke the endpoint logic immediately and end the request, so middleware that would run after routing (e.g. authentication or CORS) is skipped.<br>P3 (opt): A single route can use `.ShortCircuit()` (optionally with a status code); MapShortCircuit returns IEndpointConventionBuilder, so constraints such as host filtering can be added.<br>P4 (opt): Caveat: it doesn't affect middleware placed before UseRouting, and endpoints with [Authorize] or [RequireCors] metadata fail with InvalidOperationException. | **S1** #23 Routing in ASP.NET Core > Short-circuit middleware after routing | [P1] #23: "app.MapShortCircuit(404, "robots.txt", "favicon.ico");"<br>[P2] #23: "Use the [ShortCircuit](/en-us/dotnet/api/microsoft.aspnetcore.builder.routeshortcircuitendpointconventionbuilderextensions.shortcircuit) extension method to cause routing to invoke the endpoint logic immediately and then end the request."<br>[P2] #23: "For example, a given route might not need to go through authentication or CORS middleware."<br>[P4] #23: "The `ShortCircuit` and `MapShortCircuit` methods do not affect middleware placed before `UseRouting`. Trying to use these methods with endpoints that also have `[Authorize]` or `[RequireCors]` metadata will cause requests to fail with an `InvalidOperationException`." | ☐ |
| Q-EVAL-026 | vi | — | Project của mình target net5.0, cài JwtBearer thì bị lỗi: `NU1202: Package Microsoft.AspNetCore.Authentication.JwtBearer 8.0.5 is not compatible with net5.0 (.NETCoreApp,Version=v5.0)`. Mình nên xử lý thế nào? | **P1**: JwtBearer 8.0.5 is not compatible with net5.0, which causes NU1202.<br>**P2**: The answerer recommends moving to .NET 8 because .NET 5 is out of support.<br>P3 (opt): The latest JwtBearer for .NET 5 is 5.0.17; it is deprecated but can still be used for testing. | **S1** #15 How to create backend application using Asp.net C# Web API Core | [P1] #15: "NU1202: Package Microsoft.AspNetCore.Authentication.JwtBearer 8.0.5 is not compatible with net5.0"<br>[P2] #15: ".NET 5 is out of support, I suggest you use .net 8 instead of 5"<br>[P3] #15: "the latest version for Microsoft.AspNetCore.Authentication.JwtBearer the is 5.0.17 as below image shows, it is deprecated, but it still could be used for testing" | ☐ |
| Q-EVAL-027 | en | — | In an ASP.NET Core web API controller, what does the framework send back automatically when a posted model fails validation, and how can I change the format of that response? | **P1**: For web API controllers, MVC responds with a ValidationProblemDetails response when model validation fails.<br>**P2**: MVC builds that response with InvalidModelStateResponseFactory; it can be replaced via ConfigureApiBehaviorOptions (options.InvalidModelStateResponseFactory = ...).<br>P3 (opt): The example returns a BadRequestObjectResult with the ModelState and supports JSON and XML (AddXmlSerializerFormatters). | **S1** #12 Handle errors in ASP.NET Core APIs > Additional error handling features > Validation failure error response<br>alt S1 #12 Handle errors in ASP.NET Core APIs > Additional error handling features > Key differences for controllers | [P1] #12: "For web API controllers, MVC responds with a [ValidationProblemDetails](/en-us/dotnet/api/microsoft.aspnetcore.mvc.validationproblemdetails) response type when model validation fails."<br>[P2] #12: "MVC uses the results of [InvalidModelStateResponseFactory](/en-us/dotnet/api/microsoft.aspnetcore.mvc.apibehavioroptions.invalidmodelstateresponsefactory#microsoft-aspnetcore-mvc-apibehavioroptions-invalidmodelstateresponsefactory) to construct the error response for a validation failure."<br>[P2] #12: "options.InvalidModelStateResponseFactory = context =>" | ☐ |
| Q-EVAL-028 | vi | — | Ngay từ đầu, C# có nhắm tới việc chạy nhanh và nhẹ ngang ngửa C hay assembly không? | **P1**: No. C# applications are meant to be economical with memory and processing power, but the language was not intended to compete directly on performance and size with C or assembly language.<br>P2 (opt): Other design goals: simple, modern, general-purpose, object-oriented; strong type checking, array bounds checking, detection of uninitialized variables, automatic garbage collection. | **S1** #18 Introduction | [P1] #18: "Although C# applications are intended to be economical with regard to memory and processing power requirements, the language was not intended to compete directly on performance and size with C or assembly language." | ☐ |
| Q-EVAL-029 | en | — | In an ASP.NET Core app, when I inject my EF Core DbContext into a controller, what service lifetime does it have by default, and how do I use it to load the blog whose BlogId is 1? | **P1**: By default, EF contexts are registered with the scoped lifetime, because web app database operations are normally scoped to the client request.<br>**P2**: Load one blog with `await context.Blogs.SingleAsync(b => b.BlogId == 1)`.<br>P3 (opt): A different lifetime can be set with an AddDbContext overload. | **S1** #10 Dependency injection in ASP.NET Core > Entity Framework contexts<br>**S2** #22 Querying Data > Loading a single entity | [P1] #10: "By default, Entity Framework contexts are added to the service container using the [scoped lifetime](#service-lifetimes) because web app database operations are normally scoped to the client request."<br>[P2] #22: ".SingleAsync(b => b.BlogId == 1);" | ☐ |
| Q-EVAL-030 | vi | — | Trong tutorial unit test C# bằng dotnet test và xUnit, test `[Theory]` được thêm vào để kiểm tra IsPrime với nhiều giá trị nhỏ hơn 2 có tên là gì, và tên đó có đúng quy tắc đặt tên unit test không? | **P1**: A test name has three parts: the method being tested, the scenario, and the expected behavior.<br>**P2**: IsPrime_ValuesLessThan2_ReturnFalse follows it: IsPrime = method, ValuesLessThan2 = scenario, ReturnFalse = expected behavior.<br>P3 (opt): Good names document behavior and show which scenario fails. | **S1** #03 Unit testing best practices for .NET > Best practices > Follow test naming standards<br>**S2** #28 Unit testing C# in .NET using dotnet test and xUnit > Create a test > Add more tests | [P1] #03: "The name of your test should consist of three parts:"<br>[P1] #03: "Name of the method being tested"<br>[P1] #03: "Scenario under which the method is being tested"<br>[P1] #03: "Expected behavior when the scenario is invoked"<br>[P2] #28: "public void IsPrime_ValuesLessThan2_ReturnFalse(int value)" | ☐ |
| Q-EVAL-031 | en | — | In C#, what value does `s` hold after `string s = default;`, and why? | **P1**: `default` produces null for a string, because string is a reference type and default is null for reference types.<br>P2 (opt): For value types default is a value, e.g. 0 for numeric types and false for bool.<br>P3 (opt): Classes, arrays, delegates and strings are reference types; structs, enums and built-in numeric types are value types. | **S1** #04 Built-in types and literals > `default` expressions<br>**S2** #26 The C# type system > Value types and reference types<br>alt S2 #20 A tour of the C# language > Familiar C# features | [P1,P2] #04: "The `default` expression produces the default value for a type: `0` for numeric types, `false` for `bool`, and `null` for reference types:"<br>[P1] #26: "Classes, arrays, delegates, and strings are reference types." | ☐ |
| Q-EVAL-032 | vi | — | Mình viết một custom IExceptionHandler và muốn inject `AppDbContext` qua constructor để ghi lỗi vào database. Làm vậy có ổn không, vì sao? | **P1**: An IExceptionHandler instance has the singleton lifetime.<br>**P2**: EF contexts are registered as scoped by default.<br>**P3**: A service shouldn't use a database context whose lifetime is shorter than its own, so a singleton handler shouldn't take the scoped DbContext through its constructor. | **S1** #13 Handle errors in ASP.NET Core > IExceptionHandler<br>**S2** #10 Dependency injection in ASP.NET Core > Entity Framework contexts | [P1] #13: "The lifetime of an `IExceptionHandler` instance is singleton."<br>[P2] #10: "By default, Entity Framework contexts are added to the service container using the [scoped lifetime](#service-lifetimes) because web app database operations are normally scoped to the client request."<br>[P3] #10: "Services of a given lifetime shouldn't use a database context with a lifetime that's shorter than the service's lifetime." | ☐ |
| Q-EVAL-033 | en | — | My EF Core query is read-only: I only display the results. How do I make EF Core not keep track of the entities it returns? | Refuse: The assistant should explicitly state that the provided collection does not contain sufficient information.<br>must not claim: Any API or option for turning off tracking (e.g. a no-tracking method), presented as from the documents. | — (not in the documents) | absence proof ABS-001 (`evidence-map.yaml`) | ☐ |
| Q-EVAL-034 | vi | — | Làm sao để giới hạn một endpoint ASP.NET Core, mỗi client chỉ được gọi tối đa 100 request mỗi phút (rate limiting)? | Refuse: The assistant should explicitly state that the provided collection does not contain sufficient information.<br>must not claim: Any rate-limiting API, middleware name or policy option presented as from the documents. | — (not in the documents) | absence proof ABS-002 (`evidence-map.yaml`) | ☐ |
| Q-EVAL-035 | en | — | What does scope validation check in ASP.NET Core dependency injection, and when is it enabled? | Refuse: The assistant should explicitly state that the provided collection does not contain sufficient information.<br>must not claim: What scope validation checks, which exception it throws, or in which environment it is enabled, presented as from the documents. | — (not in the documents) | absence proof ABS-003 (`evidence-map.yaml`) | ☐ |
| Q-EVAL-036 | vi | — | Dùng Moq thì làm sao kiểm tra một method của mock được gọi đúng một lần? | Refuse: The assistant should explicitly state that the provided collection does not contain sufficient information.<br>must not claim: Any call-verification or call-count API (e.g. Verify, Times.Once) presented as from the documents. | — (not in the documents) | absence proof ABS-004 (`evidence-map.yaml`) | ☐ |

### Dev set (6, tuning only, never reported)

| ID | Lang | Group | Question | Expected answer (required points bold) | Expected source · heading path | Evidence quote(s) | OK? |
|---|---|---|---|---|---|---|---|
| Q-DEV-001 | en | — | What is a static class in C#, and can another class inherit from it? | **P1**: A static class can't be instantiated and contains only static members.<br>**P2**: A static class is implicitly sealed, so you can't derive from it. | **S1** #07 C# classes > Static classes | [P1] #07: "A `static` class can't be instantiated and contains only static members."<br>[P2] #07: "A static class is implicitly sealed. You can't derive from it or instantiate it." | ☐ |
| Q-DEV-002 | vi | — | nint và nuint trong C# là gì, và khi nào thì nên dùng chúng thay cho int hay long? | **P1**: nint and nuint are integers whose size matches the platform's native pointer size: 32 bits on a 32-bit platform, 64 bits on a 64-bit platform.<br>**P2**: They are for interop and low-level memory work; otherwise use int or long. | **S1** #04 Built-in types and literals > Numeric types > Native-sized integers<br>alt S1 #16 Integral numeric types (C# reference) > Characteristics of the integral types<br>alt S1 #16 Integral numeric types (C# reference) > Native sized integers | [P1] #04: "The `nint` and `nuint` types represent integers whose size matches the platform's native pointer size: 32 bits on a 32-bit platform, 64 bits on a 64-bit platform:"<br>[P2] #04: "Stick with `int` or `long` unless you have a specific reason to use native-sized types." | ☐ |
| Q-DEV-003 | en | — | What makes a contextual keyword in C# different from an ordinary keyword? | **P1**: A contextual keyword has a specific meaning in the code but isn't a reserved word in C#.<br>P2 (opt): Some, such as partial and where, have special meanings in two or more contexts. | **S1** #06 C# keywords > Contextual keywords<br>alt S1 #06 C# keywords | [P1] #06: "A contextual keyword provides a specific meaning in the code, but it isn't a reserved word in C#." | ☐ |
| Q-DEV-004 | vi | — | Khi nào thì được dùng `async void` trong C#, và tại sao nên tránh nó ở những chỗ khác? | **P1**: async void should only be used for event handlers, which must return void.<br>**P2**: Exceptions thrown in an async void method can't be caught outside that method; such methods are hard to test and can cause side effects if the caller doesn't expect them to be asynchronous. | **S1** #02 Asynchronous programming scenarios > Review considerations for asynchronous programming > Return 'async void' only from event handlers | [P2] #02: "Exceptions thrown in an `async void` method can't be caught outside of that method"<br>[P1] #02: "When you write asynchronous event handlers, you need to use the `async` modifier on a `void` returning method for the handlers." | ☐ |
| Q-DEV-005 | en | — | How do I issue refresh tokens and use them to renew JWT access tokens in ASP.NET Core? | Refuse: The assistant should explicitly state that the provided collection does not contain sufficient information.<br>must not claim: Any refresh-token flow presented as from the documents. | — (not in the documents) | absence proof ABS-005 (`evidence-map.yaml`) | ☐ |
| Q-DEV-006 | vi | — | Làm sao để version Web API trong ASP.NET Core, ví dụ cho v1 và v2 chạy song song? | Refuse: The assistant should explicitly state that the provided collection does not contain sufficient information.<br>must not claim: Any API-versioning package or attribute presented as from the documents.; MapGroup or a route prefix such as /v1 presented as the documents' way to version an API. | — (not in the documents) | absence proof ABS-006 (`evidence-map.yaml`) | ☐ |
<!-- CASES END -->
