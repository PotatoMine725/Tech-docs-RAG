# RAG-003 prompt log

Date: 2026-09-26 · Tool: Claude Code (CLI), Claude Sonnet 5

## Owner invocation (chat, verbatim)

```text
Run agents/prompts/08-RAG-003-resilience-cli-smoke.md (task RAG-003) with the addendum below.
The addendum overrides the prompt where they differ.

ADDENDUM (owner, 2026-09-26)

0. Git and machine:
   - Branch rag-003 from dev (1596329). Standard git block. PR into dev. Do not merge. Stop for 99-VERIFY.
   - One heavy process at a time (memory limits). A cut-off pytest run → re-run once and log it.

1. OD-11, pre-decided by the owner. Record it in ADR-0004 as an amendment:
   - Answer model: up to 3 attempts in total (1 + 2 retries) on 429 (per-minute), 503, timeouts and connection errors.
     Use exponential backoff with jitter, honor retry-after, and cap any single wait at 120 s (same cap as the embedder).
   - Then the fallback model (FALLBACK_MODEL from config, gemini-3.5-flash) gets ONE attempt.
   - Daily-quota 429: no retries. Go straight to the single fallback attempt. If that is also a quota error → raise the quota error.
   - Reuse the embedder's retry/retry-after/quota-classification helpers (RAG-001a) where possible,
     by moving them to a shared infrastructure module. Do not copy them.

2. Error classification (RAG-002 report, GUI mismatch 4):
   - No google.genai / httpx exception may escape infrastructure.
   - Wrap them into core LLMError subtypes that map 1:1 onto the GUI's AskQuestionError kinds:
     quota | unavailable (503/timeout/connection after all attempts) | other.
   - Tests prove the mapping.

3. Throttle and limits (owner, from AI Studio 2026-09-26, free tier). Put these in config:
   - gemini-3.5-flash-lite (answer + judge): RPM 15 / TPM 250K / RPD 500 → throttle at 13 RPM
   - gemini-3.5-flash (fallback):           RPM 5  / TPM 250K / RPD 20  → throttle at 4 RPM
   Fallback is a small safety net (20/day): keep the single-attempt rule from item 1.
   Add a setting ALLOW_FALLBACK (default true for the app/CLI). The eval runner will set it to false
   (see the ledger note for 09a). Test: with ALLOW_FALLBACK=false, all attempts fail → the classified error; the fallback is never called.

4. Accounting in LLMResponse / AnswerResult:
   - model_used, retry_count, fallback_used.
   - prompt/output/thoughts tokens (None when not reported).
   - Add latency_ms["retry_wait"] (the total time spent waiting in backoff), so the latency report can separate model time from waiting.

5. CLI scripts/ask.py:
   - As in the prompt, plus --gate-off, documented as a diagnostic only (never used by the eval runner).
   - Never print the key.

6. Smoke checks (live, labelled "smoke check, not evaluation data"). Use DEV questions only:
   a. Q-DEV-001 (EN) and Q-DEV-002 (VI), Arm A → cited answers
   b. Q-DEV-005, normal → refused by the gate (0 LLM requests)
   c. Q-DEV-005 --gate-off → exercises the LLM insufficient path; check the D2 behavior (missing_information, related-only citations)
   d. ONE direct call to the fallback model with Q-DEV-001's prompt → proves JSON mode + schema work on the fallback model
      (report its thoughts tokens and latency)

   Budget: ≤ 5 LLM requests, 0 embed requests (dev queries are cached; if any is not, stop and report).
   Save the outputs to validation/generation/smoke-2026-09-26.md. On a 429: stop, save the redacted body, and do not loop.

7. Offline tests: as in the prompt, plus
   - daily-quota → fallback once → quota error
   - retry-after longer than 120 s → capped
   - fallback success sets fallback_used=True and model_used=fallback
   - no raw exception escapes (parametrize over 429/503/ConnectError/ReadTimeout)

8. Eval firewall: no eval-v1 IDs or question text anywhere. Run the grep check.

9. Report docs/reports/execution/RAG-003.md with "Explain it back".
   Update ledger row 08 and AI_WORKLOG. G3 gate check at the end.

Ledger row 09a note: "Eval runs use ALLOW_FALLBACK=false so every answer and judgment comes from gemini-3.5-flash-lite
(no mixed-model results). Failed cases are recorded as error and re-run later.
The runner plans against RPD 500 (~144 requests per full 2-arm run, ~13 RPM) and records model_used per case."
```

## Task prompt (`agents/prompts/08-RAG-003-resilience-cli-smoke.md`, verbatim at the time of the run)

```text
# RAG-003 — Retry/fallback, token accounting, CLI, smoke checks (gate G3 / M2)

Read `agents/prompts/_common.md` first and follow it. Read ADR-0004 D11–D13.
Entry: RAG-002 done.

## Do
1. **OD-11** (ask user): max retries before fallback — recommend 3 attempts, exponential backoff + jitter, honor retry-after; then `gemini-3.5-flash` once. Record in ADR-0004 as an amendment.
2. Extend the basic Gemini LLM adapter from RAG-002 (`infrastructure/llm/gemini/` only): client-side throttle to the per-minute request limit (config), retry on 429/503/timeouts and connection errors (e.g. `httpx.ConnectError`/`httpx.ReadTimeout`, wrapped into a core `LLMError` subtype so callers never see raw httpx exceptions), fallback model, record `model_used`, `retry_count`, `fallback_used`, prompt/output token counts from the response usage metadata (needed for cost benchmarking later). Model names from config only.
3. CLI `scripts/ask.py "question" [--arm A|B] [--json]`: prints answer, numbered citations (doc, heading path, excerpt), insufficient flag, latency per stage, model, tokens.
4. **Smoke checks** (live, mark as "smoke check, not evaluation data") → `validation/generation/smoke-<date>.md`: 1 EN + 1 VI answerable question (NOT from the eval set), 1 out-of-corpus question → insufficient message. Paste real outputs.
5. Secret scan: key absent from repo, logs, outputs.
6. Offline tests: retry/backoff/fallback logic with a fake client that raises 503 then succeeds; a fake connection error then success (retried) and a persistent connection error (wrapped error after max attempts, no raw httpx exception escapes); fallback after N failures; token fields populated.
```

## Owner decisions during the task

- OD-11 was pre-decided in the addendum (item 1); the interactive question in prompt step 1 was therefore not asked.
- Choices the addendum leaves open are listed in ADR-0004 (amendment 2026-09-26) and in the execution report, for the owner to confirm at 99-VERIFY.
