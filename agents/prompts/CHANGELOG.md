# Prompt changelog

Every change to a file in `agents/prompts/` after the prompt set was added (`09bd22f`, 2026-09-24) is logged here: date, file, what, why, decided by.
Decisions themselves live in specs/ADRs; a prompt only follows them. Newest last.

| Date | File(s) | What | Why | Decided by | Commit |
|---|---|---|---|---|---|
| 2026-09-24 | `07-RAG-002-…`, `09a-EVAL-003a-…`, `09b-EVAL-003b-…` (+ `generation-spec.md`, `citation-spec.md`) | Back-filled. RAG-002 keeps `missing_information` and optional related citations on insufficient answers; 09a record gets `missing_information`; 09b uses evidence slots (D1) for hits and routes unanswerable-but-answered cases to a judge refusal check (D2). | Carry owner decisions D1/D2 (EVAL-001, `831d5a0`) into the downstream prompts | Owner (D1, D2); edit by Claude Code | `8d04044` |
| 2026-09-24 | `09b-EVAL-003b-…` | Back-filled (owner's uncommitted edit). Result mapping splits "insufficient" into a bare message (no judge call → `correct_refusal`) and "insufficient + related note/citations" (judge refusal check → `correct_refusal` or `hallucination`); citation metrics count only answered records, related citations counted separately as `related_citation_count`; source precision uses slots incl. alternates; two new tests (both refusal-check outcomes, slot example). | A refusal that still says "use `MapGroup("/v1")`" must be catchable as a hallucination | Owner (edit); conflict with `evaluation-spec.md:40` settled by the owner as REORIENT-001 C1 | REORIENT-001 commit 2 |
| 2026-09-24 | `README.md` | Back-filled (owner's uncommitted edit). Row 00R REORIENT-001 added to the task table. | New one-off task | Owner | REORIENT-001 commit 2 |
| 2026-09-24 | `00R-REORIENT-001-sync-state-with-prompts.md` | New prompt file (owner-written), committed unchanged. | Re-align records with the prompt set | Owner | REORIENT-001 commit 2 |
| 2026-09-24 | `09b-EVAL-003b-…` | Citation classes renamed to the spec names `correct_evidence` / `correct_source_wrong_evidence` / `unsupported_citation` / `citation_missing`. | Spec/prompt conflict C2 (spec `evaluation-spec.md` § Proposed vs 09b §4) | Owner (AskUserQuestion, REORIENT-001) | REORIENT-001 commit 2 |
| 2026-09-24 | `02-EVAL-002-…`, `09a-EVAL-003a-…`, `09b-EVAL-003b-…` | Question file, run record and judge input use the `evaluation-dataset-design.md` §18 schema: `answer_points` with `required`, `expected_sources` / `acceptable_alternate_sources` with `slot`, `evidence`, `acceptable_variations`, `must_not_claim`, `citation_criteria` (was: flat `expected_source_ids`, `expected_heading_paths`, `evidence_quotes`, `required_points`). 09b: expected-span test applies to answerable cases only, per slot. | Conflict C3: flat lists would freeze a question file without D1 slots, and the judge would not see optional points or the allowed variations (e.g. 036's `Mock<T>` remark) | Owner (AskUserQuestion, REORIENT-001) | REORIENT-001 commit 2 |
| 2026-09-24 | `09b-EVAL-003b-…` | Points-covered score added to §4 (yes = 1, partial = 0.5, optional points ignored) with a test. | Conflict C4: OD-5 decided the score but 09b omitted it, and `partial` was undefined | Owner (AskUserQuestion, REORIENT-001) | REORIENT-001 commit 2 |
| 2026-09-24 | `02-EVAL-002-…` | Entry condition: owner re-check sheet filled and EVAL-001 `verified` in the ledger. | Changed ground truth must be owner-checked and fixes re-verified before the freeze (00R step 6) | REORIENT-001 prompt (owner) | REORIENT-001 commit 2 |
| 2026-09-24 | `_common.md` | "Before you start" step 4: open the task ledger, every prerequisite must be `verified`, audit pre-existing work instead of redoing it. "When done" step 4: also update the task's ledger row. | Guard against work running outside the prompt set again | REORIENT-001 prompt (owner), wording verbatim | REORIENT-001 commit 2 |
| 2026-09-24 | `99-VERIFY.md` | The verifier may also update the task's ledger row, with a verdict → status rule; commit includes the ledger; later steps renumbered. | Otherwise nobody is allowed to set `verified`, and the `_common.md` rule could never be met | Claude Code (REORIENT-001), consequence of the owner's ledger rule — **confirmed by owner 2026-09-25** (REORIENT-001 fixes) | REORIENT-001 commit 2 |
| 2026-09-25 | `09c-EVAL-003c-…` (+ `evaluation-dataset-design.md` §18) | CSV keeps the brief's column names with an explicit mapping from the run record (`expected_source` ← `expected_sources`, `generated_answer` ← `answer`, `latency_total_ms` ← `latency_ms.total`). Design §18 result file now uses the 09a record names (`answer`, `citations[{marker,…}]`, `retry_count`, `latency_ms.total`, …) instead of `generated_answer`, `generated_citations`, `retries`. | Conflict C5, missed by REORIENT-001, found by its verify | Owner (2026-09-25: prompt names win, spec updated) | REORIENT-001 fixes |
| 2026-09-25 | `README.md` | Headless VERIFY `--allowedTools` gets `Edit(docs/plans/task-ledger.md)`; "Needs" for 02 and 03 now include "00R verified" (the ledger already required it). | Let the headless verifier do 99-VERIFY step 4; make the extra prerequisite visible where tasks are picked | Owner (ledger rights confirmed 2026-09-25); REORIENT-001 verify fixes | REORIENT-001 fixes |
| 2026-09-25 | `09b-EVAL-003b-…` (+ `evaluation-spec.md` § Retrieval hit rule) | §1: `source_hit@k` and `section_hit@k` reported **strict** (expected sources only) and **lenient** (with alternates, the D1 rule); spans in `expected-spans-v1.json` tagged `expected`/`alternate`; MRR and slot fraction stay lenient; BP-EVAL-017 listed as a multi-slot case. §5: slot test now S1 = {04 expected}, S2 = {26 expected, 20 alternate}; top-k {04, 20} → lenient hit, strict miss. | Owner's condition for accepting the widened alternates of the EVAL-001 re-check ("Other changes" table); 017 split into two slots | Owner (EVAL-001 re-check, 2026-09-25); edit by Claude Code | OWNER-001 |
| 2026-09-25 | `09b-EVAL-003b-…` (+ `evaluation-spec.md` § Retrieval hit rule) | Headline retrieval number = lenient, strict always next to it in the same table; MRR and slot fraction lenient, plus a secondary `strict MRR`, with a test case; EXP-001 must say when the arm comparison flips between strict and lenient (spec). Replaces the TBD added earlier in OWNER-001. | Open item left by the strict/lenient condition | Owner (2026-09-25) | OWNER-001 |
| 2026-09-25 | `README.md` | "Needs" for 02 now includes "INGEST-004 verified", mirroring ledger row 02. | ADR-0003 D3a changes the Arm A chunk file. The owner's Step 0 makes EVAL-002 wait for INGEST-004 to be verified. | Owner (INGEST-004 Step 0); edit by Claude Code | INGEST-004 follow-up |
| 2026-09-25 | `_common.md` | "When done" step 6: the "Explain it back" bullets are also saved in the execution report (section "Explain it back") before the commit, not only in the chat report. | INGEST-004 verify (G): chat-only bullets could not be checked by the verifier | Owner (INGEST-004 follow-up prompt); edit by Claude Code | INGEST-004 follow-up fixes |
| 2026-09-26 | `09b-EVAL-003b-…` | §1 `evidence_hit@k` and citation/evidence matching use the same whitespace normalization as `validate_questions.py` (collapse all Unicode whitespace incl. U+00A0; example Q-EVAL-028 / #18). Test to match. | Ground-truth quotes are validated with that normalization; a stricter matcher would score false misses | Owner (EVAL-002 review, 2026-09-26) | EVAL-002 owner-review commit |
