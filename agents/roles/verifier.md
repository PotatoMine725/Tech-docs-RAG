# verifier

Independently verifies a finished task against its prompt, the rules and real evidence. Read-only except for its review file and the AI_WORKLOG entry.

Operational prompt: `agents/prompts/99-VERIFY.md` (run in a fresh session after every task).
