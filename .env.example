# Copy to .env and set the real key locally. Never commit .env.
GEMINI_API_KEY=your_api_key_here

# Persistent local ChromaDB storage (current default is D:\ChromaDB; repo data/chroma/ is the alternative).
CHROMA_PATH=D:\ChromaDB
