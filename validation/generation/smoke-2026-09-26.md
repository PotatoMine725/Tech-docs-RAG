# Smoke check: RAG-003

> Label: **smoke check, not evaluation data**. Dev questions only (Q-DEV-001, Q-DEV-002, Q-DEV-005), one run per step. Latencies and token counts are single anecdotal observations, not evaluation results.

Run at 2026-09-26T13:45:21+00:00.

## Setup

- ANSWER_MODEL = `gemini-3.5-flash-lite`; FALLBACK_MODEL = `gemini-3.5-flash`; prompt `answer_v2`; gate threshold 0.686
- steps a-c: ANSWER_MAX_ATTEMPTS=1 and ALLOW_FALLBACK=false (one step = one request); step d calls the fallback model directly
- planned LLM requests: 4 (a1, a2, c on `gemini-3.5-flash-lite`; d on `gemini-3.5-flash`); b makes 0; budget 5
- throttle: 13 RPM (answer model), 4 RPM (fallback)
- embedding cache: 3 dev queries, 0 not cached → embed requests planned: 0

### a1. Answerable question, English (Arm A)

- Q-DEV-001 (en): `ask.py "<question>" --arm A`; exit code 0; LLM requests this step: 1 (used 1 of 5)

Question: What is a static class in C#, and can another class inherit from it?

````text
Question: What is a static class in C#, and can another class inherit from it?
Arm A | language en | prompt answer_v2

Answer:
A `static` class cannot be instantiated and contains only static members [1]. It is implicitly sealed, which means you cannot derive from it [1].

Insufficient information: no

Citations:
  [1] C# classes — C# classes > Static classes
      ## Static classes A `static` class can't be instantiated and contains only static members. Use static classes to organize utility methods that don't operate on instance data: ``` static class MathHelpers { public static double CircleCircumference(double radius) => 2 * Math.PI * radius;

Latency (ms): embed_query 0, retrieve 314, generate 4179, retry_wait 0, throttle_wait 0, total 4494
Model: gemini-3.5-flash-lite (retries 0, fallback no)
Tokens: prompt 1197, output 72, thoughts n/a
Retrieval: top-1 score 0.7360, threshold 0.6860, duplicates dropped 0
````

### a2. Answerable question, Vietnamese (Arm A)

- Q-DEV-002 (vi): `ask.py "<question>" --arm A`; exit code 0; LLM requests this step: 1 (used 2 of 5)

Question: nint và nuint trong C# là gì, và khi nào thì nên dùng chúng thay cho int hay long?

````text
Question: nint và nuint trong C# là gì, và khi nào thì nên dùng chúng thay cho int hay long?
Arm A | language vi | prompt answer_v2

Answer:
Các kiểu `nint` và `nuint` là các số nguyên có kích thước tùy theo nền tảng (native-sized integers) [1][2]. Kiểu `nint` là số nguyên có dấu 32-bit trong tiến trình 32-bit hoặc 64-bit trong tiến trình 64-bit, biểu diễn bên trong dưới dạng kiểu .NET `System.IntPtr` [1][3]. Kiểu `nuint` là số nguyên không dấu 32-bit trong tiến trình 32-bit hoặc 64-bit trong tiến trình 64-bit, biểu diễn bên trong dưới dạng kiểu .NET `System.UIntPtr` [1][3]. Bạn nên sử dụng chúng cho các kịch bản tương tác (interop scenarios), các thư viện cấp thấp (low-level libraries), các thao tác bộ nhớ cấp thấp, và để tối ưu hóa hiệu suất trong các trường hợp sử dụng toán học số nguyên một cách rộng rãi [1][2]. Ngược lại, bạn nên gắn bó với `int` hoặc `long` trừ khi có lý do cụ thể để dùng các kiểu có kích thước theo nền tảng này [2].

Insufficient information: no

Citations:
  [1] Integral numeric types (C# reference) — Integral numeric types (C# reference) > Characteristics of the integral types
      | `nuint` | Depends on platform (computed at runtime) | Unsigned 32-bit or 64-bit integer | System.UIntPtr | In all of the table rows except the last two, each C# type keyword from the leftmost column is an alias for the corresponding .NET type. The keyword and .NET type name are interchangeable.
  [2] Built-in types and literals — Built-in types and literals > Numeric types > Native-sized integers
      ### Native-sized integers The `nint` and `nuint` types represent integers whose size matches the platform's native pointer size: 32 bits on a 32-bit platform, 64 bits on a 64-bit platform: ``` nint bufferSize = 1024; nuint elementCount = 256; ``` You're unlikely to need `nint` or `nuint` in
  [3] Integral numeric types (C# reference) — Integral numeric types (C# reference) > Characteristics of the integral types
      ## Characteristics of the integral types C# supports the following predefined integral types: | C# type/keyword | Range | Size | .NET type | | --- | --- | --- | --- | | `sbyte` | -128 to 127 | Signed 8-bit integer | System.SByte | | `byte` | 0 to 255 | Unsigned 8-bit integer | System.Byte | |

Latency (ms): embed_query 0, retrieve 9, generate 1902, retry_wait 0, throttle_wait 0, total 1912
Model: gemini-3.5-flash-lite (retries 0, fallback no)
Tokens: prompt 2038, output 301, thoughts n/a
Retrieval: top-1 score 0.7943, threshold 0.6860, duplicates dropped 0
````

### b. Out-of-corpus question, gate on: refused by the retrieval gate

- Q-DEV-005 (en): `ask.py "<question>" --arm A`; exit code 0; LLM requests this step: 0 (used 2 of 5)

Question: How do I issue refresh tokens and use them to renew JWT access tokens in ASP.NET Core?

````text
Question: How do I issue refresh tokens and use them to renew JWT access tokens in ASP.NET Core?
Arm A | language en | prompt answer_v2

Answer:
The document collection does not contain enough information to answer this question.

Insufficient information: yes (retrieval_gate)

Latency (ms): embed_query 0, retrieve 12, total 12
Model: none (no LLM call: the retrieval gate refused)
Tokens: none
Retrieval: top-1 score 0.6741, threshold 0.6860, duplicates dropped 0
````

- gate refusal confirmed with 0 LLM requests: yes

Same question with `--json` (also 0 LLM requests):

````text
{
  "question": "How do I issue refresh tokens and use them to renew JWT access tokens in ASP.NET Core?",
  "arm": "A",
  "language": "en",
  "answer": "The document collection does not contain enough information to answer this question.",
  "insufficient": true,
  "insufficient_reason": "retrieval_gate",
  "missing_information": null,
  "citations": [],
  "llm_called": false,
  "model_used": null,
  "retry_count": null,
  "fallback_used": null,
  "tokens": {
    "prompt": null,
    "output": null,
    "thoughts": null
  },
  "latency_ms": {
    "embed_query": 0.3,
    "retrieve": 4.9,
    "total": 5.3
  },
  "retrieval": {
    "top1_score": 0.6741,
    "threshold": 0.686,
    "gate_off": false,
    "duplicates_dropped": 0
  },
  "prompt_version": "answer_v2",
  "dropped_markers": [],
  "uncited_sentences": 0
}
````

### c. Out-of-corpus question, gate off (diagnostic): the LLM's own insufficient path

- Q-DEV-005 (en): `ask.py "<question>" --arm A --gate-off`; exit code 0; LLM requests this step: 1 (used 3 of 5)

Question: How do I issue refresh tokens and use them to renew JWT access tokens in ASP.NET Core?

````text
Question: How do I issue refresh tokens and use them to renew JWT access tokens in ASP.NET Core?
Arm A | language en | prompt answer_v2
*** GATE OFF: diagnostic run, the retrieval gate is disabled. Never used for evaluation. ***

Answer:
The document collection does not contain enough information to answer this question.

Insufficient information: yes (llm)
Missing information: The provided context does not contain information about how to issue refresh tokens and use them to renew JWT access tokens in ASP.NET Core.

Latency (ms): embed_query 0, retrieve 6, generate 1312, retry_wait 0, throttle_wait 0, total 1319
Model: gemini-3.5-flash-lite (retries 0, fallback no)
Tokens: prompt 2136, output 61, thoughts n/a
Retrieval: top-1 score 0.6741, threshold off, duplicates dropped 0
````

Owner decision D2 check (facts from the model's JSON and the printed output):

- insufficient=true; printed as `Insufficient information: yes (llm)`: yes
- missing_information non-empty: yes
- cited_passages from the model: []; related-only citations printed: 0
- answer text shown is the localized message, not model text: yes

### d. Direct call to the fallback model with Q-DEV-001's exact request

- model used: `gemini-3.5-flash`; fallback_used=False; retry_count=0
- finish_reason: STOP
- tokens: prompt 1197, output 102, thoughts tokens 370
- latency: 23898 ms (one call; anecdotal)
- JSON valid: yes (parsed and checked against the answer schema): insufficient=false, cited_passages=[1]

Raw text from the fallback model:

````text
{
  "insufficient": false,
  "answer": "In C#, a static class is a class that cannot be instantiated and contains only static members [1]. It is typically used to organize utility methods that do not operate on instance data [1]. A static class is implicitly sealed, which means you cannot derive from it; therefore, another class cannot inherit from it [1].",
  "cited_passages": [1],
  "missing_information": ""
}
````

### e. The CLI as a real process (b's question; 0 LLM requests)

`python scripts/ask.py "<question>" --arm A --json` with ANSWER_MAX_ATTEMPTS=1, ALLOW_FALLBACK=false; exit code 0:

````text
{
  "question": "How do I issue refresh tokens and use them to renew JWT access tokens in ASP.NET Core?",
  "arm": "A",
  "language": "en",
  "answer": "The document collection does not contain enough information to answer this question.",
  "insufficient": true,
  "insufficient_reason": "retrieval_gate",
  "missing_information": null,
  "citations": [],
  "llm_called": false,
  "model_used": null,
  "retry_count": null,
  "fallback_used": null,
  "tokens": {
    "prompt": null,
    "output": null,
    "thoughts": null
  },
  "latency_ms": {
    "embed_query": 0.4,
    "retrieve": 131.9,
    "total": 132.4
  },
  "retrieval": {
    "top1_score": 0.6741,
    "threshold": 0.686,
    "gate_off": false,
    "duplicates_dropped": 0
  },
  "prompt_version": "answer_v2",
  "dropped_markers": [],
  "uncited_sentences": 0
}
````

## Totals

- LLM requests: 4 of 5; embedding requests: 0 (dev queries cached)

## Notes (added after the run; facts only)

- **Attempts.** A first attempt was cut off at process start by `OpenBLAS error: Memory allocation still failed after 10 retries` (the machine was low on commit memory: about 0.6 GB free virtual memory). It had sent nothing: the script prints its header before any request, and this file did not exist afterwards. The run above is the second attempt, started with `OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1`.
- **Requests actually made:** 4 LLM (3 on `gemini-3.5-flash-lite`: a1, a2, c; 1 on `gemini-3.5-flash`: d) of a budget of 5, 0 embedding requests (all 3 dev query embeddings were cached), no HTTP 429, no retry, no fallback call in a-c.
- **Code under test:** branch `rag-003` at `f011061` plus the edits to `scripts/generation/smoke_check.py` (incremental output, step e last) that were committed right afterwards in `6b05922`. `6b05922` also touched `infrastructure/gemini_retry.py` (a new `connection_error` field on `Failure`) and the embedder; only the embedder reads that field and `GeminiLLM` does not, so the LLM's behaviour was the same as in the run. Between `f011061` and `6b05922` nothing else in the LLM path changed.
- **Step c (D2).** The model answered `insufficient: true` with a non-empty `missing_information`, and listed no `cited_passages`, so no related-only citation was printed. The related-only citation branch was therefore not exercised live; it is covered offline (`tests/unit/test_ask_cli.py`, `tests/unit/application/test_answer_question.py`).
- **Step d.** `fallback_used=False` in the step d output is expected: the fallback model was called directly (as the answer model of a separate one-attempt instance), not reached through the fallback path. JSON mode and the schema work on `gemini-3.5-flash`. It reported 370 thinking tokens and took about 24 s for the answer that `gemini-3.5-flash-lite` produced in about 4 s (a1: a first call in a cold process; c and a2 took 1.3 s and 1.9 s). Single observations, not measurements. `gemini-3.5-flash-lite` reported no `thoughts_token_count` (`None`, printed `n/a`).
- **Step e.** The real `scripts/ask.py` process gave the same refusal (exit code 0, 0 LLM requests).
- Everything above is a smoke check, not evaluation data.
