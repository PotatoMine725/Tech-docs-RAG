# RAG-002 dev-set checks (2026-09-26)

Point-in-time output of `scripts/generation/dev_check.py` (appended by `--out`). Dev set only (`dev-v1.jsonl`,
6 cases: 3 EN, 3 VI, 4 answerable); no eval-set question was read, embedded or sent. n is small: every number here
is a hint for tuning, not a measured property of the system.

## Duplicate passages (offline, chunk files only)

Dedup key `passage_hash` = SHA-256 of the link-stripped chunk body the LLM sees (owner decision 2026-09-26).

### Arm A: 13 passages occur in more than one chunk, 26 extra copies (by content_hash: 2 extra copies)

| chunk ids sharing one passage (lowest id first = kept on a score tie) | same content_hash? |
|---|---|
| 12:header-1600:0002, 13:header-1600:0002 | yes |
| 12:header-1600:0009, 13:header-1600:0031 | yes |
| 13:header-1600:0041, 13:header-1600:0057 | no (link URLs differ) |
| 17:header-1600:0006, 17:header-1600:0092, 17:header-1600:0112, 17:header-1600:0122 | no (link URLs differ) |
| 17:header-1600:0016, 17:header-1600:0114, 17:header-1600:0124 | no (link URLs differ) |
| 17:header-1600:0021, 17:header-1600:0100, 17:header-1600:0116, 17:header-1600:0125 | no (link URLs differ) |
| 17:header-1600:0027, 17:header-1600:0101, 17:header-1600:0117, 17:header-1600:0126 | no (link URLs differ) |
| 17:header-1600:0033, 17:header-1600:0103, 17:header-1600:0119, 17:header-1600:0127 | no (link URLs differ) |
| 17:header-1600:0055, 17:header-1600:0088, 17:header-1600:0110, 17:header-1600:0120, 17:header-1600:0130 | no (link URLs differ) |
| 17:header-1600:0093, 17:header-1600:0113, 17:header-1600:0123 | no (link URLs differ) |
| 17:header-1600:0111, 17:header-1600:0121 | no (link URLs differ) |
| 23:header-1600:0021, 23:header-1600:0132 | no (link URLs differ) |
| 23:header-1600:0097, 23:header-1600:0131 | no (link URLs differ) |

### Arm B: 0 passages occur in more than one chunk, 0 extra copies (by content_hash: 0 extra copies)

## Dev retrieval (2026-09-26T12:05:10+00:00), top_k=5, over-fetch 10
- embedding model `gemini-embedding-001@768`; query texts 6, not cached 6 → estimated embed requests: 6 (1 request per text, V-1)
- actual embed API requests this run: 6; cache hits 6

| arm | case | lang | answerable | top-1 | top-5 scores | duplicates_dropped | top-5 chunk ids (+ duplicate_chunk_ids) |
|---|---|---|---|---:|---|---:|---|
| A | Q-DEV-001 | en | True | 0.7360 | 0.7360, 0.6867, 0.6656, 0.6617, 0.6482 | 0 | 07:header-1600:0006, 07:header-1600:0008, 07:header-1600:0001, 07:header-1600:0000, 20:header-1600:0007 |
| A | Q-DEV-002 | vi | True | 0.7943 | 0.7943, 0.7843, 0.7633, 0.7574, 0.7535 | 0 | 16:header-1600:0002, 04:header-1600:0003, 16:header-1600:0001, 16:header-1600:0008, 16:header-1600:0007 |
| A | Q-DEV-003 | en | True | 0.7737 | 0.7737, 0.7699, 0.7461, 0.7415, 0.6979 | 0 | 06:header-1600:0005, 06:header-1600:0006, 06:header-1600:0000, 06:header-1600:0001, 06:header-1600:0003 |
| A | Q-DEV-004 | vi | True | 0.7548 | 0.7548, 0.7168, 0.7042, 0.6983, 0.6911 | 0 | 02:header-1600:0016, 02:header-1600:0014, 02:header-1600:0019, 02:header-1600:0000, 02:header-1600:0005 |
| A | Q-DEV-005 | en | False | 0.6741 | 0.6741, 0.6721, 0.6681, 0.6534, 0.6533 | 0 | 15:header-1600:0009, 15:header-1600:0002, 15:header-1600:0003, 15:header-1600:0008, 15:header-1600:0001 |
| A | Q-DEV-006 | vi | False | 0.6710 | 0.6710, 0.6697, 0.6679, 0.6646, 0.6617 | 0 | 15:header-1600:0015, 23:header-1600:0113, 15:header-1600:0007, 15:header-1600:0000, 15:header-1600:0005 |
| B | Q-DEV-001 | en | True | 0.7284 | 0.7284, 0.6848, 0.6780, 0.6757, 0.6703 | 0 | 07:fixed-1600:0004, 07:fixed-1600:0005, 07:fixed-1600:0000, 07:fixed-1600:0002, 07:fixed-1600:0003 |
| B | Q-DEV-002 | vi | True | 0.7882 | 0.7882, 0.7642, 0.7569, 0.7497, 0.7225 | 0 | 16:fixed-1600:0001, 16:fixed-1600:0002, 16:fixed-1600:0006, 04:fixed-1600:0001, 16:fixed-1600:0005 |
| B | Q-DEV-003 | en | True | 0.7646 | 0.7646, 0.7401, 0.7339, 0.6968, 0.6898 | 0 | 06:fixed-1600:0004, 06:fixed-1600:0000, 06:fixed-1600:0003, 06:fixed-1600:0001, 06:fixed-1600:0002 |
| B | Q-DEV-004 | vi | True | 0.7256 | 0.7256, 0.7079, 0.7011, 0.6941, 0.6911 | 0 | 02:fixed-1600:0010, 02:fixed-1600:0000, 02:fixed-1600:0012, 02:fixed-1600:0001, 02:fixed-1600:0011 |
| B | Q-DEV-005 | en | False | 0.6714 | 0.6714, 0.6639, 0.6615, 0.6601, 0.6562 | 0 | 15:fixed-1600:0002, 15:fixed-1600:0007, 15:fixed-1600:0003, 15:fixed-1600:0001, 15:fixed-1600:0010 |
| B | Q-DEV-006 | vi | False | 0.6707 | 0.6707, 0.6671, 0.6649, 0.6598, 0.6591 | 0 | 23:fixed-1600:0206, 15:fixed-1600:0005, 15:fixed-1600:0000, 23:fixed-1600:0000, 23:fixed-1600:0137 |

- Arm A answerable top-1: min 0.7360; unanswerable top-1: 0.6741, 0.6710
- rule: threshold = min answerable top-1 − 0.05 = 0.6860; unanswerable scores stay below the lowest answerable score
- dev cases the gate would refuse at 0.6860 (Arm A): ['Q-DEV-005', 'Q-DEV-006']
- Arm A answerable top-1, en: 0.7360, 0.7737
- Arm A answerable top-1, vi: 0.7943, 0.7548

## Rendered prompt `answer_v1`: Q-DEV-001, Arm A (no LLM call)

````text
You are a documentation assistant for a fixed collection of technical documents.
Answer the QUESTION using ONLY the numbered CONTEXT passages.

Rules:
1. Use only facts stated in the CONTEXT. Do not use prior knowledge, even if you know the answer.
2. If the CONTEXT does not contain the information needed, set "insufficient": true, leave "answer" empty, and say in "missing_information" what is not covered. You may list in "cited_passages" passages that are related but do not answer the question; never present them as the answer. If the CONTEXT covers only part of the question, answer that part and say in "missing_information" what is missing.
3. End every sentence that states a fact with citation markers such as [1] or [2][3] (passage numbers), and list every passage number you cite in "cited_passages".
4. Write "answer" and "missing_information" in English. Keep code, identifiers, API and keyword names exactly as in the CONTEXT.
5. If passages describe different versions of the same feature, say which version each statement applies to; do not merge them.
6. Be concise (about 200 words max) unless a short code example from the CONTEXT is needed.

CONTEXT:
[1] C# classes — C# classes > Static classes
## Static classes

A `static` class can't be instantiated and contains only static members. Use static classes to organize utility methods that don't operate on instance data:

```
static class MathHelpers
{
    public static double CircleCircumference(double radius) =>
        2 * Math.PI * radius;
}
```

```
double circumference = MathHelpers.CircleCircumference(5.0);
Console.WriteLine($"Circumference: {circumference:F2}"); // Circumference: 31.42
```

The .NET class library includes many static classes, such as Math and Console. A static class is implicitly sealed. You can't derive from it or instantiate it.

[2] C# classes — C# classes > Inheritance
## Inheritance

Classes support *inheritance*. You can define a new class that reuses, extends, or modifies the behavior of an existing class. The class you inherit from is the *base class*, and the new class is the *derived class*:

```
var manager = new Manager("Satya", "Engineering");
Console.WriteLine($"{manager.Name} manages {manager.Department}");
// Satya manages Engineering
```

A class can inherit from one base class and implement multiple interfaces. Derived classes inherit all members of the base class except constructors. For more information, see Inheritance and Interfaces.

[3] C# classes — C# classes > When to use classes
## When to use classes

Use a class when:

- The type has complex behavior or manages mutable state.
- You need inheritance to create a base class with derived specializations, or to create a derived type that extends an existing class.
- Instances represent a shared identity, not objects that happen to hold equal values. Two references to the same object should stay in sync.
- The type is large or long-lived and benefits from heap allocation and reference semantics.

[4] C# classes — C# classes
# C# classes

Tip

**New to developing software?** Start with the Get started tutorials first. You'll encounter classes once you need to model objects with behavior and state.

**Experienced in another language?** C# classes are similar to classes in Java or C++. Skim the object initializers section for C#-specific patterns, and see Records for a data-focused alternative.

A *class* is a reference type that defines a blueprint for objects. When you create a variable of a class type, the variable holds a *reference* to an object on the managed heap. The variable doesn't hold the object data itself. Assigning a class variable to another variable copies the reference, so both variables point to the same object. Classes are the most common way to define custom types in C#. Use them when you need complex behavior, inheritance, or shared identity between references.

[5] A tour of the C# language — A tour of the C# language > Familiar C# features
that interface must provide. You can also define generic types and methods. Generics use *type parameters* to provide a placeholder for an actual type when used.

As you write code, you define functions, also called methods, as members of `struct` and `class` types. These methods define the behavior of your types. You can overload methods with different numbers or types of parameters. Methods can optionally return a value. In addition to methods, C# types can have properties, which are data elements backed by functions called *accessors*. C# types can define events, which allow a type to notify subscribers of important actions. C# supports object oriented techniques such as inheritance and polymorphism for `class` types.

C# apps use exceptions to report and handle errors. This practice is familiar if you used C++ or Java. Your code throws an exception when it can't do what was intended. Other code, no matter how many levels up the call stack, can optionally recover by using a `try` - `catch` block.

Tip

To learn more about types, methods, and exceptions, visit the C# fundamentals section. It covers the type system, object-oriented programming, and exception handling in depth.

QUESTION: What is a static class in C#, and can another class inherit from it?
````

## End to end: Q-DEV-001 (en, answerable=True), Arm A, 2026-09-26T12:06:32+00:00
- ANSWER_MODEL = `gemini-3.5-flash-lite`; prompt `answer_v1`; threshold 0.686; estimated LLM requests: 1 (0 if the gate fires)
- embedding model `gemini-embedding-001@768`; query texts 1, not cached 0 → estimated embed requests: 0 (1 request per text, V-1)
- LLM requests made: 1; finish_reason STOP; usage {'prompt_tokens': 1187, 'output_tokens': 69, 'thoughts_tokens': None}
- insufficient=False reason=None; dropped_markers=[]; uncited_sentences=0; duplicates_dropped=0; top-1 0.7360
- latency ms: embed_query 0, retrieve 100, generate 3359, total 3460

Raw JSON from the model:

```json
{
  "insufficient": false,
  "answer": "A static class cannot be instantiated and contains only static members [1]. It is implicitly sealed, meaning you cannot derive from it [1].",
  "cited_passages": [
    1
  ],
  "missing_information": ""
}
```

Answer shown to the user:

> A static class cannot be instantiated and contains only static members [1]. It is implicitly sealed, meaning you cannot derive from it [1].

| marker | chunk_id | location | excerpt (first 80 chars) |
|---:|---|---|---|
| 1 | 07:header-1600:0006 | C# classes > Static classes | ## Static classes  A `static` class can't be instantiated and contains only stat |

## End to end: Q-DEV-004 (vi, answerable=True), Arm A, 2026-09-26T12:06:46+00:00
- ANSWER_MODEL = `gemini-3.5-flash-lite`; prompt `answer_v1`; threshold 0.686; estimated LLM requests: 1 (0 if the gate fires)
- embedding model `gemini-embedding-001@768`; query texts 1, not cached 0 → estimated embed requests: 0 (1 request per text, V-1)
- LLM requests made: 1; finish_reason STOP; usage {'prompt_tokens': 1197, 'output_tokens': 165, 'thoughts_tokens': None}
- insufficient=False reason=None; dropped_markers=[]; uncited_sentences=0; duplicates_dropped=0; top-1 0.7548
- latency ms: embed_query 0, retrieve 105, generate 3080, total 3185

Raw JSON from the model:

```json
{
  "insufficient": false,
  "answer": "Bạn chỉ nên dùng `async void` cho các trình xử lý sự kiện (event handlers) [1]. Nên tránh dùng `async void` ở những chỗ khác vì các lý do sau: ngoại lệ được ném ra trong phương thức `async void` không thể được bắt ở bên ngoài phương thức đó, các phương thức `async void` rất khó kiểm thử (test), và chúng có thể gây ra các tác dụng phụ tiêu cực nếu người gọi không mong đợi chúng hoạt động bất đồng bộ [1]. Các triển khai khác của phương thức trả về `async void` cũng không tuân theo mô hình TAP [1].",
  "cited_passages": [1],
  "missing_information": ""
}
```

Answer shown to the user:

> Bạn chỉ nên dùng `async void` cho các trình xử lý sự kiện (event handlers) [1]. Nên tránh dùng `async void` ở những chỗ khác vì các lý do sau: ngoại lệ được ném ra trong phương thức `async void` không thể được bắt ở bên ngoài phương thức đó, các phương thức `async void` rất khó kiểm thử (test), và chúng có thể gây ra các tác dụng phụ tiêu cực nếu người gọi không mong đợi chúng hoạt động bất đồng bộ [1]. Các triển khai khác của phương thức trả về `async void` cũng không tuân theo mô hình TAP [1].

| marker | chunk_id | location | excerpt (first 80 chars) |
|---:|---|---|---|
| 1 | 02:header-1600:0016 | Asynchronous programming scenarios > Review considerations for asynchronous programming > Return 'async void' only from event handlers | ### Return 'async void' only from event handlers  Event handlers must declare `v |

## End to end: Q-DEV-005 (en, answerable=False), Arm A, 2026-09-26T12:07:00+00:00
- ANSWER_MODEL = `gemini-3.5-flash-lite`; prompt `answer_v1`; threshold 0.686; estimated LLM requests: 1 (0 if the gate fires)
- embedding model `gemini-embedding-001@768`; query texts 1, not cached 0 → estimated embed requests: 0 (1 request per text, V-1)
- LLM requests made: 0; finish_reason None; usage {}
- insufficient=True reason=retrieval_gate; dropped_markers=[]; uncited_sentences=0; duplicates_dropped=0; top-1 0.6741
- latency ms: embed_query 0, retrieve 98, total 98

Answer shown to the user:

> The document collection does not contain enough information to answer this question.

| marker | chunk_id | location | excerpt (first 80 chars) |
|---:|---|---|---|
